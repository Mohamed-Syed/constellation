# dependencies
node_modules/
.pnpm-store/

# builds
dist/
build/
.next/
out/
*.tsbuildinfo

# turbo
.turbo/

# env & secrets (NEVER commit)
.env
.env.*
!.env.example

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# os / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# test / coverage
coverage/
.nyc_output/

# local data
data/
tmp/

# brain (Graphify) generated output — never commit the knowledge graph
graphify-out/

# SSO test tokens (real JWTs from local Keycloak) — never commit credentials
artifacts/sso-test-tokens/
