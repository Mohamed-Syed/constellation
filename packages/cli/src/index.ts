/**
 * @constellation/cli — public surface for programmatic use (the `bin.ts`
 * executable is the CLI entry; import from here to call `generatePlugin`
 * directly, e.g. from tests or other tooling).
 */
export * from "./generate-plugin.js";
